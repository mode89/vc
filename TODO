vc
    capture audio
    process audio and feed it to the neural network
    detect output pulse
        level triggered detection
            maybe need to use integration or spiking neurons
        calculate derivative of output signal
            try derivative of FFT
    do command on audio pattern
    online training
        show dialog
        handle background sound
    run as a service
        implement server
    control via client application
        execute command by passing command-line arguments
    implement MFCC
        try DFT instead of DCT
        remove dependency to 'scikits'
    debugging
        plot network's output
grid search
    implement error detection
    show progress with wxPython
